// const obj = {
//   name: '이효석',
//   isOld: true,
// };

// function getKeyValue(obj, key) {
//   console.log(obj[key]);
// }

// getKeyValue(obj, 'name');

const obj = {
  name: '이효석',
  isOld: true,
  condition: 'good',
};

// for (let key in obj) {
//   console.log(`key 값은 ${key} 이고, value ${obj[key]} 입니다`);
// }

console.log(obj.name);
console.log(obj['name']);
console.log(obj.condition);
console.log(obj['condition']);
console.log(obj.isOld);
console.log(obj['isOld']);
console.log(obj.condition);
